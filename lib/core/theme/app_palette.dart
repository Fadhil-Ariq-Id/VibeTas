import 'package:flutter/material.dart';

class AppPalette {
  // Main Colors
  static const Color primaryRed = Color.fromRGBO(255, 0, 77, 1); // FF004D
  static const Color primaryGreen = Color.fromRGBO(6, 139, 120, 1); // 068B78

  // UI Elements
  static const Color cardHero = Color.fromRGBO(156, 171, 194, 1);
  static const Color taskLocationIcon = Color.fromRGBO(
    6,
    139,
    120,
    1,
  ); // 068B78
  static const Color taskAmount = Color.fromRGBO(217, 217, 217, 1);
  static const Color textActive = Color.fromRGBO(0, 0, 0, 1);
  static const Color textInactive = Color.fromRGBO(217, 217, 217, 1);

  // Navigation
  static const Color navbar = Color.fromRGBO(217, 217, 217, 1);

  // Buttons
  static const Color buttonPrimary = Color.fromRGBO(255, 0, 77, 1); // FF004D
  static const Color buttonHover = Color.fromRGBO(255, 0, 77, 1); // FF004D

  // Background
  static const Color backgroundColor = Color.fromRGBO(236, 240, 243, 1);

  // Background for sign up page
  static const Color signUpBackground = Color.fromRGBO(
    235,
    235,
    227,
    1,
  ); // #ebebe3

  // Sign up page specific colors
  static const Color signUpBorder = Color.fromRGBO(0, 0, 0, 1); // #000000
  static const Color signUpButtonBackground = Color.fromRGBO(
    255,
    255,
    253,
    1,
  ); // #fffffd
  static const Color signUpButtonBorder = Color.fromRGBO(
    219,
    217,
    211,
    1,
  ); // #dbd9d3
  static const Color signUpText = Color.fromRGBO(0, 0, 0, 1); // #000000
}
